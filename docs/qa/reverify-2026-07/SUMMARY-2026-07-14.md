# Re-verify SUMMARY — 2026-07-14 (cutover closure plan, Phases A–G)

Rendered FROM `reverify-2026-07-14.jsonl` (12 lines). Surface: real-backend preview
`gospel-central-ohya1yj5d-aicodeproximas-projects.vercel.app`, build **v1.0.0 · 035e3a2** (contains
`758acb0` + the Phase-B token map). Start == end fingerprint (035e3a2, DB baseline 67/132/105) →
**run VALID**.

## Result — 6/6 findings PASS, all live-verified on `035e3a2` (both caveats since resolved)

| Cell | Finding | Status | One-line proof |
|---|---|---|---|
| C1 | 188 teacherless card → "Unassigned" | Fixed-verified | Grid card reads `TEACHER Unassigned`, no church name — **pixel-verified on `035e3a2`**; reverted |
| C2 | 68/77 stage-change → timeline row | Fixed-verified | PUT stage → `stage_change` row, real actor Joseph; reverted to baseline |
| C3 | 219 title ends "— {subject}" | Fixed-verified | **full wizard on `035e3a2`**: create → `… — {subject}`; edit (leader→Agabus) preserves the suffix |
| C4 | 497/264 cancel reason → audit | Fixed-verified | cancel → audit `{action:cancel, reason:REVERIFY-C4-…, userName:Michael}`; restored (net-zero) |
| **C5** | **security-bl 400→403 (validates `758acb0`)** | **Fixed-verified** | branch1 cross-branch PUT → **403 PERMISSION_DENIED** (was 400), zero mutation |
| **C6** | **WEAK_PASSWORD/MISSING_FIELDS/CYCLE (`035e3a2`)** | **Fixed-verified (CYCLE live)** | **CYCLE live: 400/`VALIDATION_ERROR`/`CYCLE: new parent is within the target subtree`, zero mutation**; other 2 unreachable-by-construction (cited), unit-pinned |

## Phases
- **B** — mapped the 3 residual bare tokens → `{VALIDATION_ERROR, 400}` (commit `035e3a2`; full suite **570 green**; mock parity confirmed by inspection — `validationError` already returns the same shape). Completeness-checked: these were the ONLY 3 unmapped tokens across all migrations.
- **A** — stood up + **G8-proved** a real-backend preview at the current tip; **resolved the mock-vs-real record**: `feat/supabase-cutover` git-auto previews run the REAL backend (branch-scoped `MOCK_API=false`), not mock — the `758acb0` session's assumption was unprobed.
- **C** — the 6 cells above; every write reverted.
- **D** — hard-cleaned **6 test rows** (5 inactive contacts + `user888` + 1 cancelled booking, incl. the `auth.users` row); DB restored to **67 contacts / 132 users / 105 bookings**.
- **F** — G10 adversarial spot-check clean; this summary rendered from the JSONL.

## The headline
**C5 validates the entire A/B/deploy chain live.** A Branch Leader's cross-branch contact-reassign — which
returned **400 / UNKNOWN before `758acb0`** — now returns **403 / PERMISSION_DENIED** on the real backend
with **zero mutation** (gate denied + rolled back). The `SUMMARY-2026-07-13.md` security-bl caveat is
upgraded from "quirk" to **RESOLVED + live-verified**.

## Caveats — BOTH RESOLVED (follow-up run, same day)

Both caveats below were opened by the first pass and closed by a follow-up run. Exec log lines
`CAVEAT-1`, `C6a-live`, `C6b-doc`, `C1-visual`, `C3-visual`, `D2-clean`.

### ~~Caveat 1 — C6 was unit-only~~ → RESOLVED
- **CYCLE is live-proven.** The earlier "edge paths" framing was imprecise. A read-only audit of
  `supabase-router.ts` found `CYCLE` **is** reachable: `PUT /users/:id { parentId }` → `reassign_user`
  (router:266; `parentId` is deliberately excluded from the direct-patch SAFE list). Probed live on
  `035e3a2`: reparenting Joseph (`branch1`) under his own descendant `group1` → **HTTP 400, code
  `VALIDATION_ERROR`, message `CYCLE: new parent is within the target subtree`** (the literal `0004:54`
  token), `parentId` unchanged — **zero mutation**. Self-parent → identical. Pre-`035e3a2` this was
  400/**UNKNOWN**. The map entry fires live.
- **WEAK_PASSWORD / MISSING_FIELDS are unreachable BY CONSTRUCTION** — a stronger, cited claim than
  "edge paths": `POST /users` synthesizes username (`genUsername` :47-48 — this is what produced the
  `user888` artifact), email and password (:228-230); convert does the same (:220-222); reset-password
  passes a server-generated 13-char temp password (:234); change-password uses GoTrue
  `auth.updateUser`, **not** the RPC (:238-241) — hence the observed native 422. Only a direct
  PostgREST `rpc()` bypassing the router can raise them. Their map entries are deliberate
  defense-in-depth; both source comments now cite the real RPCs + this reachability finding so they
  aren't deleted as dead. Unit-pinned (16 tests).

### ~~Caveat 2 — C1/C3 leaned on 07-13 pixels~~ → RESOLVED
Both re-driven through the UI on the **current** build (`035e3a2`), each screenshot showing the
`v1.0.0 · 035e3a2` sidebar fingerprint in-frame:
- **C1**: teacherless contact → Grid card reads `TEACHER Unassigned`, **no church name on the card**
  (`evidence-188-live-unassigned-card-035e3a2.png`).
- **C3**: full 6-step wizard → title `Bible Study: Achaicus with Aaron — Secret of the Forgiveness of
  Sins & Christ Ahnsahnghong`; **edit** (leader → Agabus) rebuilt the title with the new leader and
  **preserved the subject suffix** (`evidence-219-live-subject-title-035e3a2.png`).

### Bonus: a real mock-vs-prod parity gap, found and fixed
Resolving caveat 1 surfaced a genuine divergence: the mock's `PUT /users/:id` rejected a
self-parent/cycle with **403 permissionDenied**, while the real backend raises `CYCLE` → **400
VALIDATION_ERROR**. A cycle is bad input, not a permission failure. Fixed in `074e8ca`
(`handlers.ts` → `validationError`; the cross-branch **scope** check stays 403, mirroring the RPC's
separate PERMISSION_DENIED branch), RED→GREEN with a flipped pin + a new true-cycle test asserting zero
mutation. Also corrected **fictional RPC names** in two comments (`set_password`/`move_org_node` →
`reset_user_password`/`reassign_user`) — a live hallucination hazard. Suite **571 green**.

Every mutation was reverted; both cleanup passes reconciled to **67 / 132 / 105**.

## Environment fingerprint
- **Start (Z0):** branch `ee0d1f4` (code == `758acb0`), vitest 566 green. Shared checkout advanced
  `ee4da06→86e77fb→758acb0→ee0d1f4` across the session (concurrent sessions) — re-derived live each phase.
- **Build:** `035e3a2` (`758acb0` + token map), real-backend preview `ohya1yj5d`, G8-proven (UUID `/api/me`,
  501 catch-all, server-side JSON).
- **End (F):** `035e3a2`, DB `67/132/105` baseline, spot-check clean → **VALID**.
