# Remediation re-verification — SUMMARY (2026-07-13)

Rendered FROM `remediation-verify/reverify-2026-07-13.jsonl` (21 verdict lines), not from memory.

- **Routine:** `Case Study/audit/REVERIFY-ROUTINE.md` · **Ledger:** `REMEDIATION.md`
- **Workflow:** `remediation-verify` (sequential single-browser agents)
- **Primary surface:** REAL Supabase backend preview `gospel-central-ghonounep` (build `v1.0.0 · fb13257`), login shim `admin`/`gospelseed1`. Seed-edge-case rows (mock-only) re-checked on a `NEXT_PUBLIC_MOCK_API=true` local dev mock.

## Build fingerprint (start vs end) — VALID, not invalidated

The routine invalidates a run only when the start and end build fingerprints differ (`start≠end`). Here they are **identical**:

- **Start capture:** "The build fingerprint reads clearly: `v1.0.0 · fb13257`." → `v1.0.0 · fb13257`
- **End capture:** "The sidebar build fingerprint is displayed at the bottom of the navigation. The session was already authenticated (as user Joseph, Branch Leader), so no login was needed." → `v1.0.0 · fb13257`

Both captures report the same sidebar build value `v1.0.0 · fb13257` (same version, same commit `fb13257`). `start == end`, so the deployed build did **not** drift mid-run and the run is **VALID**. The two capture strings differ only in surrounding narration (the end agent added that the session was already authenticated as Joseph, a Branch Leader) — that prose is not part of the fingerprint. There is no genuine build/commit mismatch; the run is not invalidated.

## Pass rate

**19 / 21 Fixed-verified = 90.5%** · Blocked 2 · Still-broken 0 · Regressed 0.

Both Blocked verdicts are the same untestable-on-real-backend condition (mock-only seed rows), and **both are covered Fixed-verified by their mock-local counterparts in this same run** — so every checklist branch has at least one passing verdict.

## Non-Fixed-verified verdicts (one line each)

- **seed-edge-cases — Blocked** (line 7): As-written check is untestable on the real-backend preview — the mock-only seed rows (c-68 Demas … c-71 Crispus) are absent from the real DB; 3 of the 4 dormant Admin-Contacts render branches (inactive, unassigned, retention-expired) were still proven live with synthesized real data, and the 4th (Converted badge) is covered Fixed-verified by the mock-local run below.
- **236-seed (real-backend preview) — Blocked** (line 15): The Virginia Beach full-day convention closure is a mock-only seed row absent from the real DB (REST returns only the 4 recurring service windows), so the "No availability this day" greyed-room branch can't be exercised on the real preview; covered Fixed-verified separately by the mock-local run (line 9).

## Full verdict roster (FROM the JSONL)

| # | Finding | Status | Surface |
|---|---|---|---|
| 1 | 94/96 | Fixed-verified | real preview |
| 2 | 68/77 | Fixed-verified | real preview |
| 3 | 92 | Fixed-verified | real preview |
| 4 | 102 | Fixed-verified | real preview |
| 5 | 188 | Fixed-verified | real preview |
| 6 | 78/151 | Fixed-verified | real preview |
| 7 | seed-edge-cases | **Blocked** | real preview |
| 8 | seed-edge-cases (converted badge) | Fixed-verified | local mock |
| 9 | 236-seed | Fixed-verified | local mock |
| 10 | 223 | Fixed-verified | real preview |
| 11 | 292 | Fixed-verified | real preview |
| 12 | 219 | Fixed-verified | real preview |
| 13 | 132 | Fixed-verified | real preview |
| 14 | 282 | Fixed-verified | real preview |
| 15 | 236-seed (real-backend preview) | **Blocked** | real preview |
| 16 | 509 | Fixed-verified | real preview |
| 17 | 497/264 | Fixed-verified | real preview |
| 18 | 518 | Fixed-verified | real preview |
| 19 | 516 | Fixed-verified | real preview |
| 20 | 349 | Fixed-verified | real preview |
| 21 | security-bl (BL contact-reassign scope / migration 0014) | Fixed-verified | real preview |

### Noted caveats carried by otherwise-passing verdicts

- **188:** card fix holds ("Unassigned", never `groupName`), but the CSV-import *creation* path 400s on the real backend (`createdBy:'import'` → uuid parse error) — flagged as a separate parity defect (task_630bc486), out of this finding's scope.
- **security-bl:** cross-branch reassign is correctly denied on both the dropdown (75 own-branch options, 0 Virginia Beach) and the write path (PUT rejected, zero mutation), but the denial *surfaced* as HTTP **400** (PERMISSION_DENIED body) rather than 403, due to a pre-existing app-wide bare-raise token-mapping quirk shared by many RPCs — security outcome was always correct. **→ RESOLVED 2026-07-14 by commit `758acb0`**: `pgErrorToApiError` now recovers BARE tokens too (`const token = (msg.includes(':') ? msg.split(':')[0] : msg).trim()`), mapping `PERMISSION_DENIED`→403 / `NOT_FOUND`→404 / `UNAUTHORIZED`→401 / `*_CONFLICT`→409; unit-pinned in `supabase-errors.test.ts` (RED→GREEN), full suite 566 green + clean build. The migrations were correct and were NOT touched. **Live 403 re-probe CONFIRMED 2026-07-14** (C5, `reverify-2026-07-14.jsonl`): as branch1, a cross-branch `PUT /api/contacts/:id {assignedTeacherId:<VB user>}` on the real-backend preview `ohya1yj5d` @`035e3a2` returned **403 / PERMISSION_DENIED** with zero mutation (was 400/UNKNOWN pre-`758acb0`). The 3 residual bare tokens (`WEAK_PASSWORD`/`MISSING_FIELDS`/`CYCLE`) are now ALSO mapped → `VALIDATION_ERROR`/400 (commit `035e3a2`, unit-pinned) — no longer 400/UNKNOWN. Full 2026-07-14 run: `SUMMARY-2026-07-14.md`.
- **497/264 & 292:** the 264 "morph" and the "wizard rejects past times" expectations were refuted-as-designed (Radix exit flash; retroactive entry allowed by product decision 1) — the verified behavior matches the corrected expectation.

---

## Live read-only re-confirmation pass — 2026-07-14 (direct/manual, ZERO writes)

Supplementary to the workflow roster above: a second, INDEPENDENT pass driving the SAME deployed
preview (`gospel-central-ghonounep…`, real Supabase backend, build `v1.0.0 · fb13257`) BY HAND,
read-only, making **zero writes** to the shared DB. Logins: `admin`/`gospelseed1` (Dev),
`branch1`/`gospelseed1` (BL). This does **not** change the formal 19/21 workflow count above — it
re-confirms the fixes render/behave live. Fingerprint re-read in-sidebar: `v1.0.0 · fb13257` ✅.

### Observed live — PASS (13)

| Finding | Live observation (read-only) | Result |
|---|---|---|
| 94/96 | New Contact as Dev → ASSIGNED TEACHER combobox = **73 options** (both branches) | ✅ Pass |
| 92 | Partner field: 1st Escape closed the suggestion menu (dialog stayed, focus+value kept); 2nd Escape closed the dialog | ✅ Pass |
| 102 | Abel BRANCHES: slot-0 "Manaen" = `text-purple-600` + `font-semibold`; slots 1–2 plain | ✅ Pass |
| 78/151 | Admin ▸ Contacts: "Epaphras Colossae … **Inactive** **Restore**" at **opacity 0.6** (Restore present; not clicked) | ✅ Pass |
| 282 | Calendar help dialog: `/\d+ rooms?/` → null; "every room" phrasing | ✅ Pass |
| 292 | Yesterday Day view: **288/288** slots dimmed+disabled ("This time has passed"); wizard past-time → amber "…in the past — you're recording a study that already happened" | ✅ Pass |
| 223 | Typed 6-digit year "202026" into the date field → wizard **stayed mounted, no error boundary**, still Step 1 (coerced to `0006-07-14`) | ✅ Pass |
| 132 | "Select all in Foundation" ⇄ "**Deselect all in Foundation**" (section suffix kept) — seen on 3 surfaces (contact edit, New Contact, subject picker) | ✅ Pass |
| 349 | Groups search "team leader" → "**+8 more matches — keep typing to narrow**" footer | ✅ Pass |
| 509 | "This Month" card = **313** == table set to "This Month" preset = **313** | ✅ Pass |
| 518 | "This Month" drill-down Export filename = **`gospel-central-this-month.csv`** (no `diamond-`; captured via anchor interceptor, no file saved) | ✅ Pass |
| security-bl | branch1 (Joseph, NN BL) editing Abel → teacher dropdown = **75 options, all Newport News, 0 Virginia Beach, 0 unknown** | ✅ Pass |
| 516 | Corroborated: Cancellations card = 3; live audit log has real-actor (`Michael`) cancel + restore rows | ✅ Pass (corroborated) |

### Not observable read-only on the live real backend (4) — ✅ RESOLVED 2026-07-14 (plan executed; all 4 independently live-verified via write→revert)

These require a **write** to the shared DB to exercise, so they were NOT re-driven in this zero-write
pass. Each is already Fixed-verified by the workflow (live, with revert). See
`PLAN-not-observable-2026-07-14.md` for the write-then-revert re-check plan. **DONE 2026-07-14** — all 4 re-confirmed live on real-backend preview `ohya1yj5d` @`035e3a2` (C1/C2/C3/C4 in `reverify-2026-07-14.jsonl`; see `SUMMARY-2026-07-14.md`).

| Finding | Why not read-only observable | How already verified |
|---|---|---|
| 188 (teacherless card → "Unassigned") | All 4 teacherless contacts on the real DB are **inactive** (no active grid card); creating an active one is a write | Workflow (live, reverted) + local-mock screenshot `evidence-188-unassigned-card.png` |
| 68/77 (kanban stage-drag → `stage_change` timeline row) | Requires a drag = write | Workflow (live drag + revert-drag) |
| 219 (study booking title "— {subject}") | Requires creating a booking = write | Workflow (live create+edit, then cancel-revert) |
| 497/264 (cancel reason → audit Reason row + real actor) | Requires cancelling a booking = write | Workflow (live cancel+restore); live audit log shows the append-only cancel/restore rows |

### Environment observations (no action taken)

- Shared real DB carries inert **test residue** from prior runs: 4 inactive teacherless contacts
  ("Reverify TestContact", "GuardProbe Sentinel", "Epaphras Colossae", "Phoebe Cenchreae") + one
  cancelled booking `6c6c070f`. All soft-deleted/cancelled → inert; left as-is (no live deletes).
- The two **mock-only** seed findings (converted badge, 236 VB closure) are absent from the real
  backend by design; verified on the local mock (JSONL lines 8–9), not part of this real-backend pass.
